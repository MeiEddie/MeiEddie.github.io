---
outline: deep
---

# 第一课

## 无穷小

唯一的无穷小的数：`0`，剩下的都是靠近0的过程

正负交替，不影响

xcos(x)，振幅越大，并不是趋近无穷大，即使它是无界的

# 第一章：函数与极限

## 1.1映射与函数

判断两个函数是否相等：定义域、对应法则

## 问题

### 集合可以包含集合吗

集合套集合，其概念为集族，以集合为元素的集合

### 两个集合可能有多种映射关系吗

两个集合之间通常存在多种映射关系。集合A有`m`个元素，集合B有`n`个元素，从A到B的映射总共有`n^m`个

### 数集包含什么

自然数集、整数集、有理数集、实数集、复数集等等

数集是一个不断扩充的过程，其目的是为了解决上一层运算不封闭的问题